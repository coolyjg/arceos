#include <stdio.h>
#include <sys/wait.h>

// TODO
pid_t waitpid(pid_t __pid, int *__stat_loc, int __options)
{
    printf("%s%s\n", "Error: no ax_call implementation for ", __func__);
    return 0;
}
